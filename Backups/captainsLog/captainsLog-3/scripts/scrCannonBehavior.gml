//TODO: More conditions will have to be added (not just epitaph).
if(scrCannonChance() && object_index != objEpitaph && object_index != objInfiniteRegress && object_index != objPawn) {
    scrCannonDivertPower();
}
